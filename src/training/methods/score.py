"""Placeholder entry point for score-based diffusion training."""

from __future__ import annotations

from typing import Optional, Sequence


def run(argv: Optional[Sequence[str]] = None) -> None:
    """Stub for score-based diffusion training."""
    raise NotImplementedError(
        "Score-based diffusion training is not yet implemented. "
        "Extend src/training/methods/score.py with the desired behaviour."
    )
